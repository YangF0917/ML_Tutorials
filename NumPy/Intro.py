import numpy as np

# Initializing a NumPy array
arr = np.array([-1, 2, 5], dtype=np.float32)

# Print the representation of the array
# print(repr(arr))
print(arr)

'''
without the repr, outputs "[-1.  2.  5.]"
dtype represents the data type for the elements in the array
'''